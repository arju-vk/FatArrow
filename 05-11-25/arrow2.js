let getFullName = (fs, md, ln) => {
  return fs + md + ln;
};
let msg = getFullName("Rahul", "Something", "Reddy");
console.log(msg);

//

let Plus = (one, two, three) => {
  return one + two + three;
};

let root = Plus(10, 20, 30);
console.log(root);
